# 图片生成

> 来源: https://platform.minimaxi.com/docs
> 转换时间: 2026-08-10

## 目录

- [概述](#概述)
- [根据文本生成图片](#根据文本生成图片)
- [结合参考图生成图片](#结合参考图生成图片)
- [推荐阅读](#推荐阅读)

---

## 概述

图片生成服务提供文生图（text-to-image）与图生图（image-to-image）两种核心功能。

---

## 根据文本生成图片

根据详尽的文本描述（prompt），直接生成与之匹配的图片。

### 请求示例

```python
import base64
import requests
import os

url = "https://api.minimaxi.com/v1/image_generation"
api_key = os.environ.get("MINIMAX_API_KEY")
headers = {"Authorization": f"Bearer {api_key}"}

payload = {
    "model": "image-01",
    "prompt": "men Dressing in white t shirt, full-body stand front view image :25, outdoor, Venice beach sign, full-body image, Los Angeles, Fashion photography of 90s, documentary, Film grain, photorealistic",
    "aspect_ratio": "16:9",
    "response_format": "base64",
}

response = requests.post(url, headers=headers, json=payload)
response.raise_for_status()

images = response.json()["data"]["image_base64"]
for i in range(len(images)):
    with open(f"output-{i}.jpeg", "wb") as f:
        f.write(base64.b64decode(images[i]))
```

### 生成结果

成功调用后，API 将返回 base64 编码的图片数据，可解码保存为本地 JPEG 文件。

---

## 结合参考图生成图片

此功能允许提供一张包含清晰主体的参考图（支持网络图片链接），并结合 prompt 描述，生成一张保留了主体特征的新图片。

> **注意**：当前每次请求仅支持传入一张参考图。该功能尤其适用于需要保持人物形象一致性的场景，例如为同一个虚拟角色生成不同情境下的图片。

### 请求示例

```python
import base64
import requests
import os

url = "https://api.minimaxi.com/v1/image_generation"
api_key = os.environ.get("MINIMAX_API_KEY")
headers = {"Authorization": f"Bearer {api_key}"}

payload = {
    "model": "image-01",
    "prompt": "女孩在图书馆的窗户前，看向远方",
    "aspect_ratio": "16:9",
    "subject_reference": [
        {
            "type": "character",
            "image_file": "https://example.com/reference.jpg"
        }
    ],
    "response_format": "base64",
}

response = requests.post(url, headers=headers, json=payload)
response.raise_for_status()

images = response.json()["data"]["image_base64"]
for i in range(len(images)):
    with open(f"output-{i}.jpeg", "wb") as f:
        f.write(base64.b64decode(images[i]))
```

### 生成结果

成功调用后，API 将返回 base64 编码的图片数据，可解码保存为本地 JPEG 文件。

---

## 推荐阅读

- [文生图接口](https://platform.minimaxi.com/docs) - 使用 API 接口，输入文本描述，进行图片生成
- [图生图接口](https://platform.minimaxi.com/docs) - 使用 API 接口，输入图片内容，进行图片生成
- [产品定价](https://platform.minimaxi.com/docs) - 各模型的定价说明、计费方式及使用限制
- [速率限制](https://platform.minimaxi.com/docs) - 为保证资源的高效使用，引入速率限制，以确保服务的可用性、稳定性

---

*文档整理完成 - 2026-08-10*